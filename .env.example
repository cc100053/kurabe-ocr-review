# Supabase (Kurabe project lgwdwfotnwfparvxqqnq)
SUPABASE_URL=https://lgwdwfotnwfparvxqqnq.supabase.co
# SERVICE ROLE KEY — full DB access, server-side only. NEVER prefix with NEXT_PUBLIC_.
SUPABASE_SERVICE_ROLE_KEY=

# Access control in production is handled by Vercel Authentication (Deployment
# Protection), so no app-level auth env vars are needed. For LOCAL dev the app
# is unprotected on localhost — only run it on a trusted machine.
